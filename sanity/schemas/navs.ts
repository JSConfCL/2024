import { type SchemaTypeDefinition } from "sanity";

export default {
  "name": "navs",
  "title": "Menús",
  "type": "document",
  "fields": [
    {
      "name": "title",
      "title": "Título",
      "type": "string"
    }
  ]
} as SchemaTypeDefinition;
